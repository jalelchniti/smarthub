{
  "name": "smarthub-booking-functions",
  "description": "Firebase Functions for SmartHub booking notifications",
  "scripts": {
    "build": "echo 'No build needed for JS functions'",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^11.8.0",
    "firebase-functions": "^4.3.1",
    "nodemailer": "^6.9.3"
  },
  "devDependencies": {
    "@types/nodemailer": "^7.0.1",
    "typescript": "^4.9.0"
  },
  "private": true
}
